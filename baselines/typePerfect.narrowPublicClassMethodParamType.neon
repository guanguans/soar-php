# total 1 error

parameters:
    ignoreErrors:
        -
            message: '#^Parameters should have "array" types as the only types passed to this method$#'
            count: 1
            path: ../src/Soar.php
